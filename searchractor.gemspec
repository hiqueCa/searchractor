Gem::Specification.new do |s|
  s.name        = 'searchractor'
  s.version     = '1.1.0'
  s.summary     = "A module dedicated to search algorithms studies in Ruby based on interactor's architecture"
  s.authors     = ['Henrique Caltram']
  s.files       = ['lib/searchractor.rb']

  s.add_development_dependency "rspec"
end
